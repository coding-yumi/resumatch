# 2026-06-25

## 修复 utils/ai_client.py 中文编码问题
- 问题：requests 使用 `json=` 参数发送请求时，latin-1 编码无法处理中文字符
- 修复：改用 `data=json.dumps(..., ensure_ascii=False).encode("utf-8")` 手动序列化，并在 Content-Type header 中加入 `charset=utf-8`
- 同时补充 `import json`
